apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository

metadata:
  name: gramfel
  namespace: flux-system

spec:
  image: ghcr.io/nint8835/gramfel
  interval: 1m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy

metadata:
  name: gramfel
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: gramfel
  filterTags:
    pattern: '^main-[[:xdigit:]]{7,}-(?P<ts>\d{8}-\d{6})$'
    extract: '$ts'
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: IfNotPresent
